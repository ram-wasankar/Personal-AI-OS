services:

* type: web
  name: synapse-keeper-api
  runtime: python
  rootDir: backend
  plan: free
  autoDeploy: true

  buildCommand: pip install -r requirements.txt
  startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT

  healthCheckPath: /health

  envVars:

  * key: PYTHON_VERSION
    value: 3.11.11

  * key: APP_ENV
    value: production

  * key: MONGO_URI
    value: mongodb+srv://ramwasankar_db_user:cWBFUaZX9veNLrCP@personal-ai-os.l3z5xvg.mongodb.net/?appName=Personal-AI-OS

  * key: MONGO_DB
    value: synapse_keeper

  * key: SECRET_KEY
    generateValue: true

  * key: CORS_ORIGINS
    value: personal-ai-k13nxjrp9-ram-wasankars-projects.vercel.app

  * key: OPENAI_API_KEY
    value: ""   # optional (leave empty if not using)

  * key: OPENAI_MODEL
    value: gpt-4o-mini

  * key: REDIS_URL
    value: ""   # optional

  * key: LOG_LEVEL
    value: INFO
